# --- Go ---
bin/
*.exe
*.test
*.out
coverage.out
coverage.html
vendor/
# Built binaries from `go build` without -o (e.g. `go build ./cmd/api` → ./api)
/api/api
/api/cmd/api/api

# --- Node / Vite / React ---
node_modules/
dist/
build/
.vite/
.cache/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Terraform ---
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
crash.log

# --- Ansible ---
*.retry

# --- Editor / IDE ---
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# --- Secrets / runtime ---
*.key
*.pem
*.crt
!**/testdata/**/*.crt
!**/testdata/**/*.pem
.env
.env.local
.env.*.local
secrets/

# --- SSH keys (never commit) ---
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
authorized_keys
known_hosts

# --- Vault state (lives in docker volumes; never on disk in-tree) ---
init.json
approle.json
*.unseal-keys
unseal-keys.txt

# --- Bandolier runtime artifacts (compose volumes mounted to host for dev) ---
.dev-volumes/
app.db
app.db-journal
app.db-wal
app.db-shm

# --- Logs ---
*.log
logs/

# --- OS ---
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Project process artifacts (kept local-only, not pushed to GitHub) ---
.superpowers/
.claude/
docs/superpowers/
docs/decisions-legacy.md
handoff.md
ideas.md
CLAUDE.md
